# =============================================================================
# ADCL Configuration
# =============================================================================
# This file is used by both developers and customers.
#
# Developers: Build images with ./adcl-dev build, then start with ./adcl-dev start
# Customers:  Start directly with ./adcl start (pulls pre-built images)
#
# Both use the SAME docker-compose.yml from dist/ - test what you deploy!
# =============================================================================

# =============================================================================
# LLM API Keys
# =============================================================================
# Optional: Set your Anthropic API key to use real AI
# If not set, the agent will use mock responses

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Service Port Configuration
AGENT_PORT=7000
NMAP_PORT=7003
FILE_TOOLS_PORT=7002
HISTORY_PORT=7004
ORCHESTRATOR_PORT=8000
FRONTEND_PORT=3000
REGISTRY_PORT=9000

# Service Host Configuration
SERVICE_HOST=0.0.0.0

# Network Configuration for Nmap
# Allowed networks for scanning (comma-separated CIDR ranges)
# Default: Local network and common private ranges
ALLOWED_SCAN_NETWORKS=192.168.50.0/24

# Default scan targets - Edit these to match your network
# These are used as defaults in workflows (you can still override in the UI)
DEFAULT_SCAN_NETWORK=192.168.50.0/24
DEFAULT_SCAN_TARGET=192.168.50.1

# =============================================================================
# Image Registry Configuration
# =============================================================================
# These specify which images to use from the registry.
#
# Developers: ./adcl-dev builds images and tags them as ghcr.io/adcl/*:latest
#             Docker will use the local images instead of pulling
# Customers:  ./adcl start pulls these images from ghcr.io registry
#
REGISTRY=ghcr.io/adcl
VERSION=latest

# Volume Paths (Host Side)
# These have smart defaults in docker-compose files - only override if needed
# WORKFLOWS_PATH=
# AGENT_TEAMS_PATH=
# AGENT_DEFINITIONS_PATH=
# LOGS_PATH=
# WORKSPACE_PATH=
# REGISTRY_PATH=
# TRIGGERS_PATH=
# CONFIGS_PATH=
# VOLUMES_PATH=

# Container Configuration
ORCHESTRATOR_USER=root
PYTHONUNBUFFERED=1

# API Configuration
# Browser connects to exposed port on localhost (not Docker internal hostname)
API_HOST=localhost
API_PROTOCOL=http

# Auto-install Configuration

# Container Paths (should match volume mounts)
CONTAINER_AGENT_DEFINITIONS_PATH=/app/agent-definitions
CONTAINER_AGENT_TEAMS_PATH=/app/agent-teams
CONTAINER_WORKFLOWS_PATH=/app/workflows
CONTAINER_REGISTRIES_CONF_PATH=/app/registries.conf
CONTAINER_LOGS_PATH=/app/logs
CONTAINER_LOGS_SHARED_PATH=/app/logs_shared
CONTAINER_MCP_SERVERS_PATH=/app/mcp_servers
CONTAINER_WORKSPACE_SHARED_PATH=/app/workspace_shared
CONTAINER_HISTORY_STORAGE_PATH=/app/volumes/conversations
CONTAINER_FILE_WORKSPACE_PATH=/workspace
CONTAINER_REGISTRY_DATA_PATH=/app/registries
CONTAINER_REGISTRY_DATA_V2_PATH=/app/registry

# Docker Configuration
DOCKER_SOCKET_PATH=unix:///var/run/docker.sock
DOCKER_NETWORK_NAME=mcp-network
DOCKER_NETWORK_DRIVER=bridge
DOCKER_HOST_INTERNAL=host.docker.internal
DOCKER_STOP_TIMEOUT=10

# HTTP Timeouts (in seconds)
HTTP_TIMEOUT_DEFAULT=10.0
HTTP_TIMEOUT_HEALTH_CHECK=5.0
HTTP_TIMEOUT_LONG_RUNNING=600.0

# Nmap Timeouts (in seconds)
NMAP_TIMEOUT_QUICK_SCAN=300
NMAP_TIMEOUT_FULL_SCAN=600
NMAP_TIMEOUT_VULNERABILITY_SCAN=600

# LLM Configuration
LLM_MAX_TOKENS=4096

# Polling Configuration (in seconds)
POLLING_INTERVAL=2.0
POLLING_SHORT_INTERVAL=0.1
POLLING_SUPERVISOR_INTERVAL=10.0

# Config File Paths
# Orchestrator loads its own config, MCPs load their own
ORCHESTRATOR_CONFIG_PATH=/app/configs/orchestrator.yaml
ADCL_SYSTEM_CONFIG_DIR=/configs
KALI_PORT=7005

# Vulhub Configuration
VULHUB_NETWORK_SUBNET=10.100.0.0/16
VULHUB_NETWORK_GATEWAY=10.100.0.1
